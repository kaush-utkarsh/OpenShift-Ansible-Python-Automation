# Generic
APP_NAME=hello-app
NAMESPACE=demo
IMAGE=ghcr.io/yourorg/hello-app:{{CI_COMMIT_SHA}}
REPLICAS=2
CONTAINER_PORT=8080

# OpenShift access (for local debugging via oc-login)
# Prefer injecting these as CI secrets; keep this file for local dev.
CLUSTER_API=https://api.openshift.example.com:6443
OC_TOKEN=sha256~paste_your_token
